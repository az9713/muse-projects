# Cold-start task (the one-line prompt the agent received)

> Add tags to the task tracker: tasks can carry tags; listing can filter by
> tag. Touch store, service, CLI, docs, and tests. Keep every existing
> behavior green.

The agent started with no knowledge of this repo — only this file and the
directory listing. Acceptance: `test/tags.test.mjs` (10 tests).
