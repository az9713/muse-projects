# #3 Test-and-fix swarm (both minimal, mirrors #1)

- `python/` — `src/billing.py` + weak suite (2 tests) + full suite (23 tests).
  Env in `python/.venv` (`source .venv/bin/activate`). See its README.
- `js/` — `src/pricing.mjs` + weak (2 checks) + full (23 tests). See its README.

Nothing was deleted; existing files untouched.
