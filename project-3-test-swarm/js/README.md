# #3 Test-and-fix swarm — JS (`src/pricing.mjs`)

Weak: `test/test-weak.mjs` (2 checks). Full: `test/test-full.test.mjs` (24 tests).

## Verify

```sh
cd js
node test/test-weak.mjs
node --test test/test-full.test.mjs
```
